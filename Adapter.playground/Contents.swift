/**
 `Adapter`
Usando lo aprendido con el patron `Adapter`, completa la clase `SquareToRectangleAdapter` para poder adaptar una clase `Square` y sea utilizado como un `Rectangle`.

 - Square: Representa un `cuadradro` que tiene como propiedad `lado`
 - Rectangle: Protocolo que representa un rectangulo con 3 propiedades ancho, alto y area

*/

import Foundation

class SquareToRectangleAdapter: Rectangle {

    init(_ square: Square) {
        // todo
    }
    // todo
}
